# 基于react的ts空项目模板

```高度可订制的react项目空壳```

## Framework

react + redux + typescript + webpack + less + css module + react-loadable + eslint + axios封装拦截器

## Install

```shell
npm install
npm start
```
